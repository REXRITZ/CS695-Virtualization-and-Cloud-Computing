PERMISSION ISSUES:
If permission errors occur, use following command to gain root access to execute the commands:

-> sudo su

Running run_dr_bloom.sh will build all neccessary stuff to test the program